#include<iostream>
#include<string>
#include "List.h"
#include "Queue.h"
using namespace std;

int main()
{
	char ch;
	int val;
	List tes;
	Queue test = Queue(tes);
	test.isEmpty();
	ch = s;
	val = 0;
	tes.InsertatAny(ch, val);
	val++;
	ch = a;
	tes.InsertatAny(ch, val);
	val++;
	ch = w;
	tes.InsertatAny(ch, val); val++;
	ch = b;
	tes.InsertatAny(ch, val);
	test.enqueue(tes);
	return 0;
}